rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open for all authenticated users to debug initialization
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
